# 03: The recipe and the manual skill

**What to build:** `prompt.md` (the Prompt Max recipe, with examples) and `SKILL.md` (`/prompt-max <rough prompt>` in-context path, plus how the hook's context is to be treated).

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Recipe covers rules, output shape, PASS, and four diverse examples
- [ ] SKILL.md description triggers only on explicit `/prompt-max`
